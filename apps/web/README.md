# CodeSnip 官网（Astro）

静态官网（`apps/web`），用于展示 CodeSnip 的产品介绍、特性与文档。

## 开发

在仓库根目录：

```bash
pnpm web:dev
```

或在 `apps/web` 目录：

```bash
pnpm dev
```

## 构建与产物

```bash
pnpm web:build
```

产物目录：`apps/web/dist/`
